-- -- ========================================================================
-- -- Schema Initialization Script: ConfigDB Configuration Repository
-- -- Target Database: Oracle Database 23ai / Free Edition (Pluggable Database)
-- -- Purpose: Centralized property initialization across environment profiles
-- --          (default, dev, test, prod) for the Config Server ecosystem.
-- -- ========================================================================
--
-- SET DEFINE OFF;
-- ALTER SESSION SET CURRENT_SCHEMA = SYSTEM;
--
-- -- 1. Clean Up Existing Objects (Idempotency Safe)
-- BEGIN
-- EXECUTE IMMEDIATE 'DROP TABLE CONFIG_PROPERTIES CASCADE CONSTRAINTS';
-- EXCEPTION
--     WHEN OTHERS THEN
--         IF SQLCODE != -942 THEN RAISE; END IF;
-- END;
-- /
--
-- -- 2. Create Centralized Configuration Table
-- CREATE TABLE CONFIG_PROPERTIES (
--                                    ID            NUMBER(19, 0) GENERATED BY DEFAULT ON NULL AS IDENTITY,
--                                    APPLICATION   VARCHAR2(255) NOT NULL,
--                                    PROFILE       VARCHAR2(255) NOT NULL,
--                                    LABEL         VARCHAR2(255) NOT NULL,
--                                    PROP_KEY      VARCHAR2(255) NOT NULL,
--                                    PROP_VALUE    VARCHAR2(4000),
--                                    CREATED_AT    TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
--                                    CONSTRAINT PK_CONFIG_PROPERTIES PRIMARY KEY (ID),
--                                    CONSTRAINT UC_APP_PROFILE_LABEL_KEY UNIQUE (APPLICATION, PROFILE, LABEL, PROP_KEY)
-- );
--
-- -- 3. Add Performance Optimization Indexes
-- CREATE INDEX IDX_CONFIG_FETCH ON CONFIG_PROPERTIES (APPLICATION, PROFILE, LABEL);
--
-- -- 4. Insert Comprehensive Dummy Seed Data for Profiles
-- -- ========================================================================
-- -- PROFILE: default (Shared across all fallback services unless overridden)
-- -- ========================================================================
-- INSERT INTO CONFIG_PROPERTIES (APPLICATION, PROFILE, LABEL, PROP_KEY, PROP_VALUE)
-- VALUES ('application', 'default', 'main', 'server.port', '8080');
--
-- INSERT INTO CONFIG_PROPERTIES (APPLICATION, PROFILE, LABEL, PROP_KEY, PROP_VALUE)
-- VALUES ('application', 'default', 'main', 'spring.jackson.serialization.fail-on-empty-beans', 'false');
--
-- INSERT INTO CONFIG_PROPERTIES (APPLICATION, PROFILE, LABEL, PROP_KEY, PROP_VALUE)
-- VALUES ('application', 'default', 'main', 'management.endpoints.web.exposure.include', 'health,info,refresh');
--
-- -- ========================================================================
-- -- PROFILE: dev (Local Development Context)
-- -- ========================================================================
-- -- E-Commerce User Service Configuration Matrix
-- INSERT INTO CONFIG_PROPERTIES (APPLICATION, PROFILE, LABEL, PROP_KEY, PROP_VALUE)
-- VALUES ('user-service', 'dev', 'main', 'server.port', '8081');
--
-- INSERT INTO CONFIG_PROPERTIES (APPLICATION, PROFILE, LABEL, PROP_KEY, PROP_VALUE)
-- VALUES ('user-service', 'dev', 'main', 'spring.datasource.url', 'jdbc:oracle:thin:@//localhost:1521/FREEPDB1');
--
-- INSERT INTO CONFIG_PROPERTIES (APPLICATION, PROFILE, LABEL, PROP_KEY, PROP_VALUE)
-- VALUES ('user-service', 'dev', 'main', 'spring.datasource.username', 'SYSTEM');
--
-- INSERT INTO CONFIG_PROPERTIES (APPLICATION, PROFILE, LABEL, PROP_KEY, PROP_VALUE)
-- VALUES ('user-service', 'dev', 'main', 'spring.datasource.password', 'YourSecurePassword123');
--
-- INSERT INTO CONFIG_PROPERTIES (APPLICATION, PROFILE, LABEL, PROP_KEY, PROP_VALUE)
-- VALUES ('user-service', 'dev', 'main', 'spring.jpa.hibernate.ddl-auto', 'update');
--
-- INSERT INTO CONFIG_PROPERTIES (APPLICATION, PROFILE, LABEL, PROP_KEY, PROP_VALUE)
-- VALUES ('user-service', 'dev', 'main', 'logging.level.org.hibernate.SQL', 'DEBUG');
--
-- -- Core Feature Flags for Dev
-- INSERT INTO CONFIG_PROPERTIES (APPLICATION, PROFILE, LABEL, PROP_KEY, PROP_VALUE)
-- VALUES ('user-service', 'dev', 'main', 'app.feature.new-ui-enabled', 'true');
--
-- -- ========================================================================
-- -- PROFILE: test (Continuous Integration / QA Context)
-- -- ========================================================================
-- INSERT INTO CONFIG_PROPERTIES (APPLICATION, PROFILE, LABEL, PROP_KEY, PROP_VALUE)
-- VALUES ('user-service', 'test', 'main', 'server.port', '8081');
--
-- INSERT INTO CONFIG_PROPERTIES (APPLICATION, PROFILE, LABEL, PROP_KEY, PROP_VALUE)
-- VALUES ('user-service', 'test', 'main', 'spring.datasource.url', 'jdbc:oracle:thin:@//test-db-cluster:1521/TESTPDB');
--
-- INSERT INTO CONFIG_PROPERTIES (APPLICATION, PROFILE, LABEL, PROP_KEY, PROP_VALUE)
-- VALUES ('user-service', 'test', 'main', 'spring.datasource.username', 'QA_USER');
--
-- INSERT INTO CONFIG_PROPERTIES (APPLICATION, PROFILE, LABEL, PROP_KEY, PROP_VALUE)
-- VALUES ('user-service', 'test', 'main', 'spring.datasource.password', 'TestEnvSecret556');
--
-- INSERT INTO CONFIG_PROPERTIES (APPLICATION, PROFILE, LABEL, PROP_KEY, PROP_VALUE)
-- VALUES ('user-service', 'test', 'main', 'spring.jpa.hibernate.ddl-auto', 'validate');
--
-- INSERT INTO CONFIG_PROPERTIES (APPLICATION, PROFILE, LABEL, PROP_KEY, PROP_VALUE)
-- VALUES ('user-service', 'test', 'main', 'logging.level.org.hibernate.SQL', 'INFO');
--
-- INSERT INTO CONFIG_PROPERTIES (APPLICATION, PROFILE, LABEL, PROP_KEY, PROP_VALUE)
-- VALUES ('user-service', 'test', 'main', 'app.feature.new-ui-enabled', 'false');
--
-- -- ========================================================================
-- -- PROFILE: prod (High Availability Production Context)
-- -- ========================================================================
-- INSERT INTO CONFIG_PROPERTIES (APPLICATION, PROFILE, LABEL, PROP_KEY, PROP_VALUE)
-- VALUES ('user-service', 'prod', 'main', 'server.port', '80');
--
-- INSERT INTO CONFIG_PROPERTIES (APPLICATION, PROFILE, LABEL, PROP_KEY, PROP_VALUE)
-- VALUES ('user-service', 'prod', 'main', 'spring.datasource.url', 'jdbc:oracle:thin:@//prod-scan-listener.internal:1521/PRODPDB_SERVICE');
--
-- INSERT INTO CONFIG_PROPERTIES (APPLICATION, PROFILE, LABEL, PROP_KEY, PROP_VALUE)
-- VALUES ('user-service', 'prod', 'main', 'spring.datasource.username', 'PROD_APP_OWNER');
--
-- -- Utilizing robust external secrets mapping fallback pattern for production passwords
-- INSERT INTO CONFIG_PROPERTIES (APPLICATION, PROFILE, LABEL, PROP_KEY, PROP_VALUE)
-- VALUES ('user-service', 'prod', 'main', 'spring.datasource.password', '${PROD_DB_SECRET_PASS}');
--
-- INSERT INTO CONFIG_PROPERTIES (APPLICATION, PROFILE, LABEL, PROP_KEY, PROP_VALUE)
-- VALUES ('user-service', 'prod', 'main', 'spring.jpa.hibernate.ddl-auto', 'none');
--
-- INSERT INTO CONFIG_PROPERTIES (APPLICATION, PROFILE, LABEL, PROP_KEY, PROP_VALUE)
-- VALUES ('user-service', 'prod', 'main', 'logging.level.root', 'WARN');
--
-- INSERT INTO CONFIG_PROPERTIES (APPLICATION, PROFILE, LABEL, PROP_KEY, PROP_VALUE)
-- VALUES ('user-service', 'prod', 'main', 'app.feature.new-ui-enabled', 'false');
--
-- -- 5. Commit Transactions to Disk
-- COMMIT;
--
-- PROMPT ConfigDB initialization complete with sample matrices successfully loaded.