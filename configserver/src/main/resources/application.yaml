spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: "https://github.com/royarghyadip6/app-configuration"
          default-label: main
          username: ${GIT_USERNAME}
          password: ${GIT_TOKEN}
server:
  port: 8085
  
  