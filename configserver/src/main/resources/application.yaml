spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: "https://github.com/royarghyadip6/app-configuration"
          default-label: main
server:
  port: 8085
  
  